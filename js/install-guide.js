(()=>{let promptEvent=null;const guide=document.getElementById('smInstallGuide');if(!guide)return;const install=document.getElementById('smInstallNow');const later=document.getElementById('smInstallLater');const ios=document.getElementById('smInstallIOS');const android=document.getElementById('smInstallAndroid');const isIOS=/iPad|iPhone|iPod/.test(navigator.userAgent)||(/Macintosh/.test(navigator.userAgent)&&navigator.maxTouchPoints>1);const isStandalone=matchMedia('(display-mode: standalone)').matches||navigator.standalone===true;const mobile=matchMedia('(max-width: 900px)').matches||/Android|iPhone|iPad|iPod/i.test(navigator.userAgent);if(isStandalone)return;if(isIOS){ios.hidden=false;android.hidden=true;install.textContent='طريقة التثبيت';}const show=()=>{if(mobile&&localStorage.getItem('sm_install_guide_seen')!=='1')guide.classList.add('show')};window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();promptEvent=e;install.textContent='تثبيت التطبيق';show()});window.addEventListener('appinstalled',()=>{localStorage.setItem('sm_install_guide_seen','1');guide.classList.remove('show')});install.addEventListener('click',async()=>{if(promptEvent){promptEvent.prompt();await promptEvent.userChoice;promptEvent=null;localStorage.setItem('sm_install_guide_seen','1');guide.classList.remove('show');return}install.textContent=isIOS?'Safari ← مشاركة ← Add to Home Screen':'Chrome ⋮ ← Install app';});later.addEventListener('click',()=>{localStorage.setItem('sm_install_guide_seen','1');guide.classList.remove('show')});setTimeout(show,1000);})();