button[data-vfull].is-fullscreen{background:linear-gradient(135deg,rgba(77,164,255,.32),rgba(74,93,255,.28));color:#fff;box-shadow:0 0 18px rgba(77,164,255,.18)}
.site-video-controls button[data-vfull].is-fullscreen:after{content:""}

/* V16 — YouTube-only HD boost + supported manual quality fallback */
.site-video-player.playing:not(.native-quality-mode) .site-video-crop iframe{
  position:absolute!important;
  left:50%!important;
  top:50%!important;
  width:1280px!important;
  height:720px!important;
  max-width:none!important;
  max-height:none!important;
  transform:translate(-50%,-50%) scale(var(--sm-hd-scale,.85));
  transform-origin:center center;
}
.site-video-quality-exit{display:none;position:absolute;z-index:12;top:12px;left:12px;min-height:38px;padding:0 14px;border-radius:11px;border:1px solid rgba(116,178,255,.32);background:rgba(3,13,31,.9);color:#dcecff;font:800 11px/1 Cairo,sans-serif;backdrop-filter:blur(14px);cursor:pointer;box-shadow:0 12px 30px rgba(0,0,0,.28)}
.site-video-quality-exit:hover{background:rgba(10,34,75,.95);border-color:rgba(116,178,255,.55)}
.site-video-player.native-quality-mode .site-video-crop{inset:0;z-index:9}
.site-video-player.native-quality-mode .site-video-crop iframe{position:absolute!important;inset:0!important;width:100%!important;height:100%!important;max-width:none!important;max-height:none!important;transform:none!important;pointer-events:auto!important}
.site-video-player.native-quality-mode .site-video-edge,
.site-video-player.native-quality-mode .site-video-progress,
.site-video-player.native-quality-mode .site-video-controls,
.site-video-player.native-quality-mode .site-video-brand{opacity:0!important;pointer-events:none!important}
.site-video-player.native-quality-mode .site-video-quality-exit{display:block}
.site-video-player.native-quality-mode:after{display:none}
.site-video-player.native-quality-mode{background:#000}
@media(max-width:760px){.site-video-quality-exit{top:8px;left:8px;min-height:34px;padding-inline:10px;font-size:10px}}

/* V20 — StudyMate logo header integration */
.brand.brand-logo{
  width:148px;
  height:44px;
  min-width:148px;
  padding:0 4px;
  overflow:hidden;
  border-radius:12px;
  display:flex;
  align-items:center;
  justify-content:center;
  background:transparent;
}
.brand-logo-img{
  display:block;
  width:100%;
  height:100%;
  object-fit:contain;
  object-position:center;
  border-radius:10px;
  filter:drop-shadow(0 0 10px rgba(70,150,255,.18));
}
@media(max-width:1180px){
  .brand.brand-logo{width:118px;min-width:118px;height:40px}
}
@media(max-width:700px){
  .brand.brand-logo{width:96px;min-width:96px;height:38px}
}
